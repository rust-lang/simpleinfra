---
vars_rustup_version: "1.27.1"
vars_rustup_checksum: "32a680a84cf76014915b3f8aa44e3e40731f3af92cd45eb0fcc6264fd257c428"

vars_team_login_path: "/root/team_login"
allow_ssh_extra_groups: "dev-desktop-allow-ssh"

# Filesystem quota per user in GB
vars_user_quota_gb: 150

# Prototype user whose file system quota will be copied to new user accounts
vars_user_quota_prototype_user: "quota-prototype"

vars_user_config:
  - username: gh-Alexendoo
    shell: /usr/bin/zsh
  - username: gh-jdno
    shell: /usr/bin/zsh
  - username: gh-WaffleLapkin
    shell: /usr/bin/fish
  - username: gh-vincenzopalazzo
    shell: /usr/bin/zsh
  - username: gh-thomcc
    shell: /usr/bin/fish
  - username: gh-jieyouxu
    shell: /usr/bin/fish
